<!DOCTYPE html>
<html>
<head>
  <title>Data Viewer</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>Server: <%= host.hostname %> (<%= host.ip %>)</h1>
  <h2>Fetched Data</h2>
  <ul>
    <% data.forEach(item => { %>
      <li><strong>ID:</strong> <%= item.id %> — <strong>Name:</strong> <%= item.name %></li>
    <% }) %>
  </ul>
</body>
</html>
